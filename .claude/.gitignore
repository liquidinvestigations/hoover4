# The shared wiring is tracked: the two symlinks into .agents/ and the settings that
# declare the hooks. Everything else here is one machine's local state.
*
!.gitignore
!skills
!rules
!settings.json
settings.local.json
