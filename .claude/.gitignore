# The shared wiring is tracked: the two symlinks into .agents/, the role definitions, and the
# settings that declare the hooks. Everything else here is one machine's local state.
*
!.gitignore
!skills
!rules
!agents
!agents/**
!settings.json
settings.local.json
