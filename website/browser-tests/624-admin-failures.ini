
; Grouped failure list with the seeded failcap rows present.
[admin-failures]
summary = Exercises the admin failures list with rows present.
url = /admin/failures
full_page = true
actions =
    wait_css #x-failures-filter-collection
    wait_css #x-failures-count-0
    sleep 800
