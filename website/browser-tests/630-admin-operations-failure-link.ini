
; Operations outcome links through to the failed reruns import tree.
[admin-operations-failure-link]
summary = Exercises the operations-page link into a captured failure tree.
url = /admin/operations
full_page = true
actions =
    wait_css #x-ops-filter-collection
    eval const el=document.querySelector('#x-ops-filter-collection');const s=Object.getOwnPropertyDescriptor(window.HTMLSelectElement.prototype,'value').set;s.call(el,'reruns');el.dispatchEvent(new Event('change',{bubbles:true}));return {ok:true};
    wait_eval const id={{operation_id}};return {ok:[...document.querySelectorAll('tr[data-op-id]')].some(r=>r.dataset.opId===id&&r.querySelector('.x-ops-failure-link')),op_id:id};
    eval const id={{operation_id}};const row=[...document.querySelectorAll('tr[data-op-id]')].find(r=>r.dataset.opId===id);row.querySelector('.x-ops-failure-link').click();return {ok:true,op_id:id};
    wait_css #x-failures-tree
    wait_eval const id={{operation_id}};return {ok:document.body.innerText.includes(id),op_id:id};
    sleep 800
