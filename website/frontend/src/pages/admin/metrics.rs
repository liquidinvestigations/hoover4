//! Stub for Plan 2 — `/admin/metrics`. Replace this body; keep the component name.

use dioxus::prelude::*;

#[component]
pub fn AdminMetricsPage() -> Element {
    rsx! {
        Title { "Admin — Metrics" }
        div {
            style: "padding: 24px; color: #6B7280;",
            "Metrics page — Plan 2 implements this."
        }
    }
}
