# AI Server Configuration
AI_SERVER_HOST=0.0.0.0
AI_SERVER_PORT=8821
AI_SERVER_PORT_2=8822
AI_SERVER_PORT_3=8823

# Hugging Face Configuration
# HUGGING_FACE_HUB_TOKEN=your_token_here

# GPU Configuration
# CUDA_VISIBLE_DEVICES=0

# Performance Optimization (for embeddings server)
AI_SERVER_OPTIMAL_BATCH_SIZE=32
AI_SERVER_MAX_SEQUENCE_LENGTH=512
AI_SERVER_ENABLE_HALF_PRECISION=true
AI_SERVER_ENABLE_TORCH_COMPILE=true

# ----------------------------------------------------------

# DuckDuckGo Search Server Configuration
DDG_SERVER_HOST=0.0.0.0
DDG_SERVER_PORT=8889
DDG_SERVER_NAME=ddgs
DDG_SERVER_INSTRUCTIONS=Web search and news search capabilities using DuckDuckGo.

# Default search region (e.g., 'us-en', 'uk-en', 'wt-wt' for worldwide)
DDG_DEFAULT_REGION=wt-wt

# Default maximum number of search results (1-50)
DDG_DEFAULT_MAX_RESULTS=3

# Default safe search level ('strict', 'moderate', 'off')
DDG_DEFAULT_SAFESEARCH=off

# Default time limit for searches ('d' for day, 'w' for week, 'm' for month, 'y' for year)
# Leave empty for no time limit
DDG_DEFAULT_TIMELIMIT=

# DuckDuckGo client configuration
DDG_PROXY_URL=
DDG_TIMEOUT=30

# ----------------------------------------------------------

# Milvus Vector Database Configuration
MILVUS_HOST = "http://10.69.70.3"
MILVUS_PORT = "19530"
MILVUS_COLLECTION_NAME = "rag_chunks"

# Embedding Server Configuration
EMBEDDING_SERVER_URL=http://10.69.69.8:8000/v1
EMBEDDING_MODEL=intfloat/multilingual-e5-large-instruct
EMBEDDING_DIM=1024

# AI Service URLs
NER_SERVER_URL=http://10.69.69.8:8000/v1
RERANKER_SERVER_URL=http://10.69.69.8:8000/v1

# Search Configuration
SEARCH_MODE=hybrid
SEARCH_INITIAL_K=120
SEARCH_MAX_RESULTS=3
SEARCH_MAX_ALLOWED_RESULTS=100
SEARCH_RRF_K=60

# Milvus MCP Server Configuration
MILVUS_MCP_HOST=0.0.0.0
MILVUS_MCP_PORT=18081
MILVUS_MCP_SERVER_NAME=hoover4_milvus_search
MILVUS_MCP_SERVER_INSTRUCTIONS=Advanced document search over internal data not available to the public. All queries must be well phrased search text, a sentence describing the content of the document you need is better than just a keyword.

# ----------------------------------------------------------

# WHOIS MCP Server Configuration
WHOIS_SERVER_HOST=0.0.0.0
WHOIS_SERVER_PORT=8092
WHOIS_SERVER_NAME=whois_lookup
WHOIS_SERVER_INSTRUCTIONS=Domain registration information lookup with structured data output for domain analysis.

# WHOIS Configuration
# Rate limiting configuration (recommended to avoid overwhelming WHOIS servers)
WHOIS_RATE_LIMITING=true
WHOIS_RATE_LIMIT_MS=100

# Request configuration
WHOIS_TIMEOUT=30.0
WHOIS_MAX_RETRIES=3

# Logging Configuration

# Notes:
# - Rate limiting is highly recommended when performing WHOIS lookups
# - Different WHOIS servers have different rate limits and policies
# - Some servers may block excessive requests
# - The minimum wait time of 100ms helps ensure respectful usage
# - For bulk operations, consider increasing the rate limit wait time

# ----------------------------------------------------------

# Wikipedia MCP Server Configuration
WIKIPEDIA_SERVER_HOST=0.0.0.0
WIKIPEDIA_SERVER_PORT=8093
WIKIPEDIA_SERVER_NAME=hoover4_wikipedia_search
WIKIPEDIA_SERVER_INSTRUCTIONS=Wikipedia article and summary search.

# Wikipedia Configuration
# Language code for Wikipedia (ISO language code)
WIKIPEDIA_LANGUAGE=en

# Search results configuration
WIKIPEDIA_MAX_RESULTS=3
WIKIPEDIA_MAX_ALLOWED_RESULTS=50

# Summary configuration
WIKIPEDIA_SUMMARY_SENTENCES=5
WIKIPEDIA_MAX_SUMMARY_SENTENCES=10

# Supported Wikipedia languages (for reference):
# en - English
# es - Spanish
# fr - French
# de - German
# it - Italian
# pt - Portuguese
# ru - Russian
# ja - Japanese
# zh - Chinese
# ar - Arabic
# hi - Hindi
# And many more... Use wikipedia_language tool to get full list

# ----------------------------------------------------------

# Research Agent Configuration
# LLM Configuration
LLM_API_KEY=sk-12349876243249846213498432198491556
LLM_MODEL=hosted_vllm/Qwen/Qwen3-4B
LLM_TEMPERATURE=0.0
LLM_BASE_URL=http://10.69.70.5:4000/v1

# Full Research Agent Configuration (access to all MCP servers)
FULL_RESEARCH_AGENT_HOST=0.0.0.0
FULL_RESEARCH_AGENT_PORT=9090
FULL_RESEARCH_AGENT_NAME=Full Research Agent
FULL_RESEARCH_AGENT_SYSTEM_PROMPT=You are a comprehensive research assistant that can access various tools and databases to help users find information and answer questions. Your purpose is to search as many times as needed to answer the user query. Doing multiple searches in parallel is desired. Be as exhaustive in your research as possible, find as many documents that are relevant. Make a plan for the research, go in depth after finding a piece of information from a search. The result should be an exhaustive report on the topic dictated by the user. Be verbose and thorough.
FULL_RESEARCH_AGENT_MCP_SERVERS=http://hoover4-mcp-ddg:8080/mcp,http://hoover4-mcp-milvus:8081/mcp,http://hoover4-mcp-whois:8082/mcp,http://hoover4-mcp-wikipedia:8083/mcp

# Internal Search Agent Configuration (access only to Milvus)
INTERNAL_SEARCH_AGENT_HOST=0.0.0.0
INTERNAL_SEARCH_AGENT_PORT=9099
INTERNAL_SEARCH_AGENT_NAME=Internal Search Agent
INTERNAL_SEARCH_AGENT_SYSTEM_PROMPT=You are an internal search assistant that can access the vector database to help users find relevant information from the indexed knowledge base. Your purpose is to search as many times as needed to answer the user query. Doing multiple searches in parallel is desired.
INTERNAL_SEARCH_AGENT_MCP_SERVERS=http://hoover4-mcp-milvus:8081/mcp

# ----------------------------------------------------------

# Langfuse Configuration (optional - for tracing and monitoring)
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_HOST=https://cloud.langfuse.com

# Shared Configuration
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
