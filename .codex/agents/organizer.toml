name = "organizer"
description = "Coordinates planned passes, reviews and Git checkpoints. Use when a plan names the organizer role."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Read `.claude/agents/organizer.md` before you start. That file holds the rules for this role.
"""
