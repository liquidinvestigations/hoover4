name = "executor-heavy"
description = "Applies a correction package or a selected original work package and runs its named checks."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Read `.claude/agents/executor-heavy.md` before you start. That file holds the rules for this role.
"""
