{
  "_comment": "This file defines Cursor models and the concurrency cap. The renderer copies models into generated agent files. The hook adapter reads the cap.",
  "max_concurrent_subagents": 2,
  "passthrough_subagent_types": ["explore", "shell", "bash", "browser"],
  "models": {
    "organizer": "grok-4.6[effort=high]",
    "executor-light": "composer-2.5[fast=false]",
    "executor-heavy": "grok-4.6[effort=medium]",
    "reviewer": "grok-4.6[effort=xhigh]"
  },
  "agent_extra": {
    "reviewer": {
      "readonly": true
    }
  }
}
