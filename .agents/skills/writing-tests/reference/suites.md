# The suites, measured

The measured table for every suite, with path, kind, how it runs, and what it
does not cover, is
[`docs/quality-assurance/Test_Suites.md`](../../../../docs/quality-assurance/Test_Suites.md).

This file is a pointer so the counts exist in one place.
