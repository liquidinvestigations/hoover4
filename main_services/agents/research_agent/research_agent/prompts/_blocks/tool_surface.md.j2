{# The tool section, generated rather than written.

   `tool_surface` is built by the loader from the tool names that one model call binds,
   so this block can neither describe a tool that is absent nor omit one that is present.
#}
Your tools, and when to reach for each:

{{ tool_surface }}
