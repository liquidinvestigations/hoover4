# Keep the build context to the two files the image needs. The repo root holds
# testdata/ (hundreds of MB) and plans/tmp/ reference repos; a widened context would
# silently ship both into every build.
*
!ner_spacy.py
!Dockerfile
