# Operations

How to run the stack.

- `Deploying.md`, `./deploy`, its flags, and what a deploy does not do
- `Configuration_Reference.md`, `hoover4.ini` keys and the consumer of each
- `Remote_Hosts.md`, the demo box and the GPU box
- `Single_Sign_On.md`, the headers a reverse proxy must assert in front of hoover4
- `Nomad_Cluster.md`, the local Nomad, Consul and Vault cluster, and GPU support by architecture
- `Troubleshooting.md`, symptom to mechanism, for a human reading cold
- `ClickHouse_Queries.md`, the recurring diagnostic queries
