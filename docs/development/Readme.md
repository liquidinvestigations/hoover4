# Development

How to change the system.

- `Repo_Map.md`, where things are, and which file answers which question
- `Working_With_Agents.md`, the agent configuration: core, skills, rules, hooks
- `Choosing_A_Model.md`, the two model roles, and the gates that qualify one
- [Quality assurance](../quality-assurance/Readme.md), the suites, the browser cases, and what each check covers
- `Documentation_Standards.md`, how documentation and comments are written here
- `Known_Defects.md`, behaviour confirmed wrong or confirmed deliberate, with the command that shows it

Procedure an agent loads mid-task lives in `.agents/skills/` instead; these pages are the
explanation a person reads before there is a task.
