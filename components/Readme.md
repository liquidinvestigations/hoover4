# Components

Third-party front-end components the website embeds, together with the scripts that build
them and copy their artefacts into the website's assets. Nothing here is imported as a
package: the build output is checked in where the website expects to find it.

- `pdf-viewer/` — the embedded PDF viewer
